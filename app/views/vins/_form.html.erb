<%= form_for @vin, html: { multipart: true } do |f| %>
  <% if @vin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vin.errors.count, "error") %> prohibited this vin from being saved:</h2>

      <ul>
      <% @vin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
